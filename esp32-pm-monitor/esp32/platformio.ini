[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

; Tell PlatformIO where the sketch lives (default is src/)
src_dir = pm_monitor

lib_deps =
    bblanchon/ArduinoJson @ ^7.0.0

monitor_speed = 115200
upload_speed  = 921600

build_flags =
    -DCORE_DEBUG_LEVEL=3
